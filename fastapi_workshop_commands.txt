# FASTAPI WORKSHOP COMMANDS

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python -m uvicorn app:app --reload

docker buildx create --use
docker buildx build --platform linux/amd64 -t fastapi-employee --load .
docker tag fastapi-employee DOCKER_USERNAME/fastapi-employee:latest
docker push DOCKER_USERNAME/fastapi-employee:latest
